function Header() {
    return (
        <header>
            <span>My First APP React JS </span>
            <nav>
                <a href="#"> Home</a>
                <a href="#"> Sobre</a>

            </nav>
        </header>
    );
}

export default Header;